cmake_minimum_required(VERSION 3.10)
project(BlahBlahChat)

set(CMAKE_CXX_STANDARD 17)

include_directories(
    src/anim
    src/util
    src/users
    src/chats
    src/system
    src/storage
)

file(GLOB_RECURSE SOURCES
    src/*.cpp
    src/main.cpp
)

add_executable(BlahBlahChat ${SOURCES})
